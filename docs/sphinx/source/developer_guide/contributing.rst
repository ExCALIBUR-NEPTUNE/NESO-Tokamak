Contributing
=========================

Non-trivial merge requests should be into the dev branch.
Member variables should be indicated with either the "m\_" prefix, or "this->" (the former is used by Nektar++ and the latter by NESO-Particles).
There is a clang-format file for formatting.
