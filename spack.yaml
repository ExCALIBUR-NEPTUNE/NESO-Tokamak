# This is a Spack Environment file.
#
# It describes the set of packages to be installed when developing,
# along with configuration settings.
spack:
  # add package specs to the `specs` list
  specs:
  - pk.penknife ^vantage.vantagereactions~enable_tests  ^neso-particles~build_tests ^neso.adaptivecpp compilationflow=ompaccelerated ^neso+libonly
  view: true
  concretizer:
    unify: when_possible
  repos:
  - ./repo
  - ./NESO/NESO-Spack
  - ./VANTAGE-Reactions/VANTAGE-repo
  packages:
    c:
      require:
        - gcc
    cxx:
      require:
        - gcc
    all:
      providers:
        sycl: [adaptivecpp]
        mpi: [mpich, openmpi]
  develop:
    nektar:
      path: ./NESO/nektar
      spec: nektar@master
    neso:
      path: ./NESO
      spec: neso@working
    neso-particles:
      path: ./NESO/neso-particles
      spec: neso-particles@working
    vantagereactions:
      path: ./VANTAGE-Reactions
      spec: vantagereactions@working
    penknife:
      path: ./
      spec: penknife@working
